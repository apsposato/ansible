---
# tasks file for create project

- name: Create a project
  openshift_raw:
    api_version: v1
    verify_ssl: no
    username: "{{ oc_username }}"
    password: "{{ oc_password }}"
    host: "{{ api_url }}"
    kind: Project
    name: "{{ ocproject }}"
    state: present